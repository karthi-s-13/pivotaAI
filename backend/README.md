# Pivota Backend

> Pivota Data Navigator — *Find where your data lives*

## Setup

```bash
# Install dependencies
pip install -e .

# Run the server
uvicorn app.main:app --reload --port 8000
```

## Environment

Copy `.env` and update values for your local setup.
